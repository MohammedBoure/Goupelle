
-----

init commit 
-----

refactor(ui): reorder input fields to prioritize weight

- Moved the "Weight" input field to the top of the QGridLayout for a faster user workflow.
- Updated the `keyPressEvent` logic to follow the new sequence: Weight -> Gold -> Silver -> Print.
- Set the default focus to the weight input field on application startup and after successful printing.
-----

- Implement real-time gold/silver weight conversion based on reference purity.
- Add SQLite database integration to store print history with unique IDs.
- Develop a dedicated History Dialog with date filtering and pagination.
- Enhance UI/UX with a modern theme, responsive layouts, and RTL support.
- Add configurable reference purity and ID placement in settings.